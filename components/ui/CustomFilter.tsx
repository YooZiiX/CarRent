import { CustomFilterProps } from "@/types";
import Image from "next/image";

const SearchFilter = ({ title }: CustomFilterProps) => {
  return <div>SearchFilter</div>;
};

export default SearchFilter;
